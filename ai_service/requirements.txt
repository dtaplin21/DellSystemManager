# AI Service Dependencies - Production Optimized
# Core AI and ML libraries
openai>=1.0.0

# Hybrid AI Architecture dependencies (Cloud-only)
# CrewAI 0.5.0 requires langchain 0.1.x and older langchain-openai
crewai>=0.5.0,<0.6.0
langchain>=0.1.0,<0.2.0  # CrewAI 0.5.0 requires langchain 0.1.x
langchain-openai>=0.0.2,<0.0.3
# langchain-community removed - not used in codebase and conflicts with crewai 0.5.0's langchain==0.1.0 requirement

# Database and caching
redis>=5.0.0

# Web framework and utilities
flask>=3.0.0
flask-cors>=4.0.0
fastapi>=0.104.0
uvicorn>=0.24.0
python-dotenv>=1.0.0

# Data processing
pandas>=2.0.0
numpy>=1.24.0,<2.0.0  # Limit to numpy 1.x for better compatibility
pydantic>=2.0.0,<3.0.0

# File handling and utilities
python-multipart>=0.0.6
aiofiles>=23.0.0
PyMuPDF>=1.23.0  # For PDF processing
openpyxl>=3.1.0  # For Excel processing

# Browser automation
playwright>=1.42.0
nest-asyncio>=1.6.0  # Allows nested event loops (fixes CrewAI threading conflicts)

# Logging and monitoring
structlog>=23.0.0
requests>=2.31.0

# Token counting - langchain-openai 0.0.2 requires tiktoken<0.6.0
# Keeping at 0.5.2 to satisfy langchain-openai constraints
# Note: litellm (used by CrewAI) may show warnings but should still function
tiktoken>=0.5.2,<0.6.0

# Development and testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
black>=23.0.0
flake8>=6.0.0
