This directory contains small fixture images used by Playwright E2E tests (e.g. defect detection).

If you run the defect-detection tests, ensure the following files exist:
- `test-defect-image.jpg`
- `test-clean-image.jpg`

These files are intentionally not committed automatically here; you can add small sample images
or update the tests to download/generate fixtures as needed.


